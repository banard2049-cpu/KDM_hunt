{
  "name": "kdm-hunt",
  "version": "1.2.2",
  "description": "KDM 自动狩猎工具（狩猎纯净版）— Android offline app via Capacitor",
  "private": true,
  "main": "index.html",
  "scripts": {
    "test": "node scripts/check-bone-eaters.cjs && node scripts/check-forest-gate.cjs && node scripts/check-flower-start.cjs && node scripts/check-fan-decks.cjs && node scripts/check-loot-optional-resource.cjs && echo 'All hunt feature checks passed' && node scripts/check-showdown.cjs && node scripts/check-showdown-fan.cjs && node scripts/check-showdown-start-toggle.cjs && node scripts/check-showdown-layout.cjs && node scripts/check-showdown-align.cjs && node scripts/check-showdown-rule-toggle.cjs && python scripts/check-showdown-host.py",
    "android:version": "node tools/android/scripts/android-version.cjs",
    "web:stage": "node scripts/stage-web.cjs",
    "android:sync": "npm run web:stage && capacitor sync android && node tools/android/scripts/install-showdown-android.cjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "qrcode-generator": "1.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
