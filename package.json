{
  "name": "kdm-hunt",
  "version": "1.1.0",
  "description": "KDM 自动狩猎工具（狩猎纯净版）— Android offline app via Capacitor",
  "private": true,
  "main": "index.html",
  "scripts": {
    "test": "echo \"Development checks moved to .maintenance/scripts/\"",
    "android:version": "node scripts/android-version.cjs",
    "web:stage": "node scripts/stage-web.cjs",
    "android:sync": "npm run web:stage && capacitor sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
